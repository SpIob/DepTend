#!/usr/bin/env node
/**
 * deptend CLI — Phase 4
 *
 * Placeholder entrypoint. The CLI is implemented in Phase 4.
 * Running this before Phase 4 prints a status message and exits cleanly.
 */

console.error(
  "deptend CLI is not yet implemented. It ships in Phase 4 of the roadmap."
);
process.exit(1);
